import { useSearchParams,Link } from 'react-router-dom';import SiteHeader from '@/components/SiteHeader';import SiteFooter from '@/components/SiteFooter';import ArticleCard from '@/components/ArticleCard';import SEO from '@/components/SEO';import { useSearchArticles,useCategories } from '@/hooks/use-articles';import { Search as SearchIcon,ArrowLeft,ArrowRight } from 'lucide-react';import { useState } from 'react';
const SearchPage=()=>{const [sp,setSp]=useSearchParams();const initial=sp.get('q')||'';const [q,setQ]=useState(initial);const {data:results,isLoading}=useSearchArticles(initial);const {data:categories}=useCategories();const submit=(e:React.FormEvent)=>{e.preventDefault();setSp({q})};return <div className="min-h-screen editorial-shell"><SEO title="Search the archive" description="Search Juba Chronicle's public news archive." canonical="https://jubachronicles.com/search" noindex/><SiteHeader/><main className="relative z-10 container py-8 md:py-14"><Link to="/" className="inline-flex items-center gap-2 font-mono text-[10px] uppercase hover:text-primary"><ArrowLeft className="w-4 h-4"/>Front page</Link><header className="border-b-4 border-foreground py-7"><h1 className="font-display text-4xl md:text-7xl uppercase">Search the archive</h1></header><form onSubmit={submit} className="flex mt-8 max-w-4xl"><div className="relative flex-1"><SearchIcon className="absolute left-4 top-1/2 -translate-y-1/2 w-5 h-5"/><input value={q} onChange={e=>setQ(e.target.value)} placeholder="Title, keyword, or tag" className="w-full h-14 bg-card border-2 border-foreground pl-12 pr-4 text-lg focus:outline-none focus:ring-4 focus:ring-primary/20"/></div><button className="w-14 h-14 grid place-items-center bg-foreground text-background" aria-label="Search"><ArrowRight/></button></form><div className="flex flex-wrap gap-2 py-6 border-b border-border">{(categories||[]).map(c=><button key={c.id} onClick={()=>{setQ(c.name);setSp({q:c.name})}} className="border border-foreground px-3 py-1.5 font-mono text-[9px] uppercase hover:bg-primary hover:text-primary-foreground hover:border-primary">{c.name}</button>)}</div>{initial&&<div className="section-kicker mt-10 mb-6"><span>{isLoading?'Searching':`${results?.length||0} results`}</span><span>{initial}</span></div>}<div className="grid sm:grid-cols-2 lg:grid-cols-3 gap-x-7 gap-y-12">{(results||[]).map(a=><ArticleCard key={a.id} article={a}/>)}</div>{initial&&!isLoading&&!results?.length&&<p className="py-20 font-mono text-xs uppercase text-center">No matching stories found.</p>}</main><SiteFooter/></div>};export default SearchPage;
