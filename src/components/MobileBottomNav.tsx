import { Link,useLocation } from 'react-router-dom';
import { Home,Search,Grid3X3,User } from 'lucide-react';
import { useAuth } from '@/contexts/AuthContext';
import { motion } from 'framer-motion';
const MobileBottomNav=()=>{const l=useLocation();if(l.pathname.startsWith('/admin'))return null;const {user}=useAuth();const items=[{to:'/',icon:Home,label:'Home'},{to:'/search',icon:Search,label:'Search'},{to:'/search?explore=1',icon:Grid3X3,label:'Explore'},{to:user?'/dashboard':'/auth',icon:User,label:user?'Profile':'Sign in'}];const active=(to:string)=>to==='/'?l.pathname==='/':l.pathname===to.split('?')[0];return <nav className="fixed bottom-0 left-0 right-0 z-50 md:hidden bg-primary text-primary-foreground rounded-t-2xl shadow-[0_-10px_30px_-14px_hsl(var(--brand-navy)/0.6)]"><div className="flex justify-around px-1 pt-2 pb-[max(.45rem,env(safe-area-inset-bottom))]">{items.map(item=>{const Icon=item.icon;const on=active(item.to);return <Link key={item.to} to={item.to} className="relative min-w-16 flex flex-col items-center gap-0.5 py-1.5">{on&&<motion.i layoutId="mobile-nav" className="absolute -top-1 h-1 w-10 rounded-full bg-accent"/>}<Icon className={`w-5 h-5 ${on?'text-accent':'text-primary-foreground/55'}`}/><span className={`text-[8px] font-extrabold uppercase tracking-[0.14em] ${on?'text-primary-foreground':'text-primary-foreground/55'}`}>{item.label}</span></Link>})}</div></nav>};
export default MobileBottomNav;
